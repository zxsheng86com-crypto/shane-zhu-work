import { notFound } from 'next/navigation'; import { Footer, Header, Media } from '../../site';
const content={brand:['Brand','Strategy-led identities that define, differentiate, and endure.',['Brand Strategy','Naming','Tone of Voice & Messaging','Visual Identity','Brand Architecture','Brand Guidelines']],digital:['Digital','Products and websites that scale, perform, and resonate.',['Design Systems','Product Strategy','Interactive Experiences','Marketing Websites','UX & UI Design','E-commerce']],campaign:['Campaign','Creative work that earns attention, builds connection, and drives action.',['Campaign Strategy','Creative Direction','Brand Launch Campaigns','Content Production','Social Media & Activation','Art Direction']]};
export function generateStaticParams(){return Object.keys(content).map(slug=>({slug}))}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=content[slug as keyof typeof content];if(!item)notFound();return <><Header/><main><section className="service-hero"><p>Services / {item[0]}</p><h1>{item[1] as string}</h1></section><Media className="service-cover charcoal" label={`${item[0]} service image`}/><section className="service-detail"><h2>{item[0]}</h2><ul>{(item[2] as string[]).map(x=><li key={x}>{x}</li>)}</ul></section></main><Footer/></>}
