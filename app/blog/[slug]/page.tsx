import { notFound } from 'next/navigation'; import Link from 'next/link'; import { Footer, Header, posts } from '../../site';
export function generateStaticParams(){return posts.map(({slug})=>({slug}))}
export default async function Post({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=posts.find(item=>item.slug===slug);if(!post)notFound();return <><Header/><main><article className="article"><p>{post.kind} · {post.date}</p><h1>{post.title}</h1><div className="article-body"><p className="lead">The best systems leave enough room for real life.</p><p>This article page is ready for your writing. Use short sections, images and specific examples to turn a working observation into something useful for readers.</p><h2>Begin with the actual problem</h2><p>Good work becomes clearer when the process is described without theatre. Explain what changed, what constrained the work and why the final choice held.</p><Link href="/blog">← Back to all notes</Link></div></article></main><Footer/></>}
